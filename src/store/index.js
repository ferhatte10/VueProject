import { createStore } from 'vuex'
import {ref} from "vue";

export default createStore({
  state: {
    booksAdded : ref([]),
    booksAvailable : ref([])


  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
